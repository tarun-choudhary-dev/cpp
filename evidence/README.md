# Local test evidence

The browser tests write JSON results in this directory. Results can include C++ source, diagnostics, stdout, stderr and local environment details, so the JSON files are ignored by Git. Run `npm test` and `npm run test:worker` to regenerate them locally.
